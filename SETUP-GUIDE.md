# 📋 คู่มือติดตั้งแบบละเอียด - ระบบขอความช่วยเหลือน้ำท่วม

คู่มือนี้จะพาคุณทำตามทีละขั้นตอน เหมาะสำหรับผู้ที่ไม่มีพื้นฐานทางเทคนิค

---

## ⏱️ เวลาที่ใช้: 15-20 นาที

---

## 📝 สิ่งที่ต้องเตรียม

- [ ] บัญชี Google (Gmail)
- [ ] เครื่องคอมพิวเตอร์หรือมือถือที่เชื่อมต่ออินเทอร์เน็ต
- [ ] เบราว์เซอร์ (แนะนำ Chrome หรือ Firefox)

---

## ขั้นตอนที่ 1: สร้าง Google Sheet (5 นาที)

### 1.1 เปิด Google Sheets

1. ไปที่ https://sheets.google.com
2. เข้าสู่ระบบด้วยบัญชี Google
3. คลิกปุ่ม **+ Blank** (สีเขียว) เพื่อสร้างไฟล์ใหม่

### 1.2 ตั้งชื่อ Spreadsheet

1. คลิกที่ "Untitled spreadsheet" (มุมซ้ายบน)
2. เปลี่ยนชื่อเป็น: **ระบบขอความช่วยเหลือ - น้ำท่วม**
3. กด Enter

### 1.3 คัดลอก Sheet ID

1. ดูที่ URL บนเบราว์เซอร์:
   ```
   https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
                                        นี่คือ SHEET_ID ของคุณ
   ```

2. **คัดลอกส่วนที่ขีดเส้นใต้** (ตั้งแต่ `/d/` จนถึง `/edit`)
3. วางใส่ในไฟล์ Notepad หรือที่จดบันทึก (เราจะใช้ในขั้นตอนต่อไป)

**ตัวอย่าง:**
```
SHEET_ID = 1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
```

✅ **Checkpoint:** คุณมี Sheet ID แล้ว → ไปขั้นตอนต่อไป

---

## ขั้นตอนที่ 2: ตั้งค่า Google Apps Script (7 นาที)

### 2.1 เปิด Script Editor

1. ใน Google Sheet ที่เพิ่งสร้าง
2. ไปที่เมนู **Extensions** (ส่วนขยาย)
3. คลิก **Apps Script**
4. จะเปิดหน้าต่างใหม่ขึ้นมา

### 2.2 ลบโค้ดเดิมและวางโค้ดใหม่

1. ลบโค้ดเดิมที่มีอยู่ทั้งหมด (ประมาณ 5 บรรทัด)
2. เปิดไฟล์ `google-apps-script.js` ที่คุณดาวน์โหลดมา
3. คัดลอกโค้ดทั้งหมด (Ctrl+A แล้ว Ctrl+C)
4. วางใน Apps Script Editor (Ctrl+V)

### 2.3 แก้ไข SHEET_ID

1. หาบรรทัดที่เขียนว่า:
   ```javascript
   const SHEET_ID = 'YOUR_GOOGLE_SHEET_ID_HERE';
   ```

2. แทนที่ `YOUR_GOOGLE_SHEET_ID_HERE` ด้วย Sheet ID ที่คัดลอกไว้
   ```javascript
   const SHEET_ID = '1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms';
   ```

3. **ตรวจสอบให้แน่ใจว่า:**
   - มีเครื่องหมาย `'` (single quote) ทั้ง 2 ข้าง
   - ไม่มีช่องว่างข้างหน้าหรือข้างหลัง
   - มีเครื่องหมาย `;` ปิดท้าย

### 2.4 บันทึกโปรเจกต์

1. คลิกปุ่ม **💾 Save** (หรือกด Ctrl+S)
2. ตั้งชื่อโปรเจกต์: **Flood Help API**
3. คลิก **OK**

### 2.5 Deploy เป็น Web App

1. คลิกปุ่ม **Deploy** (มุมบนขวา)
2. เลือก **New deployment**
3. คลิกไอคอน **⚙️ Settings** (ตรงเฟือง) ข้าง "Select type"
4. เลือก **Web app**
5. กรอกข้อมูล:

   **Description:**
   ```
   Flood Help API v1
   ```

   **Execute as:**
   - เลือก **Me** (ชื่อของคุณ)

   **Who has access:**
   - เลือก **Anyone** ⚠️ สำคัญมาก!

6. คลิก **Deploy**

### 2.6 อนุญาตสิทธิ์

1. จะมีหน้าต่างขึ้นมาขออนุญาต
2. คลิก **Authorize access**
3. เลือกบัญชี Google ของคุณ
4. จะมีคำเตือนว่า "Google hasn't verified this app"
   - คลิก **Advanced**
   - คลิก **Go to [ชื่อโปรเจกต์] (unsafe)**
   - คลิก **Allow**

### 2.7 คัดลอก Web App URL

1. หลังจาก Deploy สำเร็จ จะมี Popup แสดง URL
2. **คัดลอก URL ทั้งหมด** (จะยาวมาก)
   ```
   https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx/exec
   ```
3. วางใส่ในไฟล์จดบันทึก (เราจะใช้ในขั้นตอนต่อไป)

### 2.8 ทดสอบว่าทำงาน

1. คัดลอก URL ที่ได้ไปวางในเบราว์เซอร์ใหม่
2. กด Enter
3. ควรเห็นข้อความ:
   ```
   🆘 ระบบขอความช่วยเหลือ - Google Apps Script
   ✅ ระบบทำงานปกติ
   ```

✅ **Checkpoint:** คุณมี Web App URL แล้ว → ไปขั้นตอนต่อไป

---

## ขั้นตอนที่ 3: ตั้งค่าไฟล์เว็บ (3 นาที)

### 3.1 แก้ไขไฟล์ app.js

1. เปิดไฟล์ `app.js` ด้วย Text Editor (Notepad, VS Code, หรืออะไรก็ได้)
2. หาบรรทัดที่ 2-3:
   ```javascript
   const CONFIG = {
       GOOGLE_SCRIPT_URL: 'YOUR_GOOGLE_APPS_SCRIPT_URL_HERE',
   };
   ```

3. แทนที่ `YOUR_GOOGLE_APPS_SCRIPT_URL_HERE` ด้วย URL ที่คัดลอกไว้
   ```javascript
   const CONFIG = {
       GOOGLE_SCRIPT_URL: 'https://script.google.com/macros/s/AKfycbxxx.../exec',
   };
   ```

4. บันทึกไฟล์ (Ctrl+S)

✅ **Checkpoint:** ไฟล์ app.js แก้ไขเรียบร้อย → ไปขั้นตอนต่อไป

---

## ขั้นตอนที่ 4: สร้าง Icons (5 นาที)

### วิธีที่ 1: ใช้ Tool Online (แนะนำ)

1. ไปที่ https://www.pwabuilder.com/imageGenerator
2. อัพโหลดรูปโลโก้ของคุณ (หรือใช้รูปอะไรก็ได้)
3. คลิก **Generate**
4. ดาวน์โหลดไฟล์ และแตกไฟล์
5. หาไฟล์:
   - `windows11/Square150x150Logo.scale-100.png` → เปลี่ยนชื่อเป็น `icon-192.png`
   - `windows11/Square150x150Logo.scale-400.png` → เปลี่ยนชื่อเป็น `icon-512.png`
6. วางไฟล์ทั้ง 2 ในโฟลเดอร์เดียวกับ `index.html`

### วิธีที่ 2: ใช้ Canva

1. ไปที่ https://www.canva.com
2. สร้างดีไซน์ขนาด 512x512 px
3. ออกแบบไอคอน (เช่น เครื่องหมาย SOS, หยดน้ำ, ฯลฯ)
4. ดาวน์โหลดเป็น PNG
5. ใช้ Tool Resize (เช่น https://imageresizer.com) ทำขนาด 192x192
6. ตั้งชื่อเป็น `icon-192.png` และ `icon-512.png`

### วิธีที่ 3: ใช้ Emoji (เร็วที่สุด)

1. ไปที่ https://emoji-to-image.com
2. เลือก Emoji ที่ต้องการ (เช่น 🆘, 💧, 🚁)
3. ดาวน์โหลดขนาด 192x192 และ 512x512
4. ตั้งชื่อตามที่กำหนด

✅ **Checkpoint:** มีไฟล์ icon-192.png และ icon-512.png แล้ว → ไปขั้นตอนต่อไป

---

## ขั้นตอนที่ 5: Deploy เว็บ (5 นาที)

### วิธีที่ 1: Netlify (แนะนำสำหรับมือใหม่)

1. ไปที่ https://www.netlify.com
2. คลิก **Sign up** (สมัครด้วย GitHub หรือ Email)
3. หลังล็อกอิน จะเห็น **Drag and drop** zone
4. ลากโฟลเดอร์ทั้งหมด (ที่มีไฟล์ index.html, app.js, ฯลฯ) ไปวาง
5. รอสักครู่ (ประมาณ 30 วินาที)
6. จะได้ URL แบบ: `https://random-name-12345.netlify.app`

**ปรับแต่ง URL (Optional):**
1. คลิก **Site settings**
2. ไปที่ **Change site name**
3. ตั้งชื่อใหม่ เช่น: `flood-help-thailand`
4. จะได้ URL: `https://flood-help-thailand.netlify.app`

### วิธีที่ 2: Vercel

1. ไปที่ https://vercel.com
2. คลิก **Sign up** (สมัครด้วย GitHub)
3. คลิก **Add New** → **Project**
4. เลือก **Upload** → เลือกโฟลเดอร์
5. คลิก **Deploy**
6. จะได้ URL แบบ: `https://your-project.vercel.app`

### วิธีที่ 3: GitHub Pages

1. สร้าง Repository ใหม่บน https://github.com
2. Upload ไฟล์ทั้งหมด
3. ไปที่ **Settings** → **Pages**
4. Source: เลือก **main branch**
5. คลิก **Save**
6. รอ 2-3 นาที จะได้ URL: `https://yourusername.github.io/repository-name`

✅ **Checkpoint:** ได้ URL ของเว็บแล้ว → ไปขั้นตอนสุดท้าย

---

## ขั้นตอนที่ 6: ทดสอบระบบ (3 นาที)

### 6.1 ทดสอบบนคอมพิวเตอร์

1. เปิด URL ที่ได้จากขั้นตอนที่ 5
2. คลิกปุ่ม **🎯 ส่งตำแหน่งของฉัน**
3. เบราว์เซอร์จะขออนุญาต → คลิก **Allow**
4. รอประมาณ 5-10 วินาที
5. ควรเห็นข้อความ **✓ ตรวจจับตำแหน่งสำเร็จ!**
6. กรอกจำนวนคน (เช่น ผู้ใหญ่ 2, เด็ก 1)
7. คลิก **📤 ส่งคำขอความช่วยเหลือ**
8. ควรเห็นข้อความ **✓ ส่งคำขอความช่วยเหลือสำเร็จ!**

### 6.2 ตรวจสอบ Google Sheets

1. กลับไปที่ Google Sheet ที่สร้างไว้
2. ควรเห็นข้อมูลปรากฏในตาราง:
   - เลขที่: 1
   - วันที่/เวลา: (เวลาปัจจุบัน)
   - Latitude/Longitude: พิกัดของคุณ
   - จำนวนคน: ตามที่กรอก

3. คลิกที่ลิงก์ **📍 ดูแผนที่** → ควรเปิด Google Maps แสดงตำแหน่งของคุณ

### 6.3 ทดสอบบนมือถือ

1. เปิด URL บนมือถือ (ใช้ Chrome หรือ Safari)
2. ทดสอบเหมือนขั้นตอนที่ 6.1
3. ลองติดตั้งเป็น App:
   - **iOS**: กดปุ่ม Share → Add to Home Screen
   - **Android**: กดเมนู → Add to Home Screen

✅ **ทุกอย่างทำงาน!** 🎉

---

## 🎯 สิ่งที่คุณได้

✅ Web Application ที่:
- ตรวจจับตำแหน่ง GPS อัตโนมัติ
- ส่งข้อมูลไปยัง Google Sheets
- ใช้งานบนมือถือและคอมพิวเตอร์
- ฟรี 100%
- ไม่มีค่าใช้จ่าย
- รองรับผู้ใช้จำนวนมาก

---

## 🔧 หากมีปัญหา

### ปัญหา: ตรวจจับตำแหน่งไม่ได้

**แนวทางแก้ไข:**
1. ตรวจสอบว่าใช้ **HTTPS** (URL ขึ้นต้นด้วย `https://`)
2. ตรวจสอบว่าได้อนุญาตสิทธิ์ในเบราว์เซอร์แล้ว
3. ลองเปิดหน้าต่างใหม่ (New Tab) และทดสอบอีกครั้ง

### ปัญหา: ส่งข้อมูลแล้วไม่เข้า Google Sheets

**แนวทางแก้ไข:**
1. ตรวจสอบ URL ใน `app.js` อีกครั้ง
2. ตรวจสอบว่า Deploy แบบ **Anyone** แล้ว
3. ลองเปิด URL ของ Google Apps Script โดยตรง (ควรเห็นข้อความ "ระบบทำงานปกติ")

### ปัญหา: Icons ไม่แสดง

**แนวทางแก้ไข:**
1. ตรวจสอบว่าไฟล์ชื่อถูกต้อง: `icon-192.png` และ `icon-512.png`
2. ตรวจสอบว่าอัพโหลดไฟล์ไปด้วย
3. ลอง Hard Refresh: Ctrl+Shift+R (หรือ Cmd+Shift+R บน Mac)

---

## 📞 ต้องการความช่วยเหลือ?

หากติดปัญหาหรือมีข้อสงสัย:
1. อ่าน README.md เพิ่มเติม
2. ดูตัวอย่างโค้ดอีกครั้ง
3. ลองทำตามทีละขั้นตอนช้าๆ

---

## 🎓 เรียนรู้เพิ่มเติม

หากต้องการปรับแต่งหรือเพิ่มฟีเจอร์:
- ดูคอมเมนต์ในโค้ด (มีคำอธิบายทุกส่วน)
- ดู README.md ส่วน "การปรับแต่ง"
- ศึกษา Google Apps Script: https://developers.google.com/apps-script

---

**ขอให้โชคดีกับโปรเจกต์!** 💪

ระบบนี้สร้างขึ้นเพื่อช่วยเหลือผู้ประสบภัย หวังว่าจะเป็นประโยชน์ 🙏