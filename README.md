# 🆘 ระบบขอความช่วยเหลือสถานการณ์น้ำท่วม

Web Application สำหรับการขอความช่วยเหลือในสถานการณ์ฉุกเฉินน้ำท่วม พร้อมระบบตรวจจับตำแหน่งอัตโนมัติที่แม่นยำสูง

## ✨ คุณสมบัติเด่น

### 🎯 ตรวจจับตำแหน่งอัตโนมัติ
- ใช้ HTML5 Geolocation API พร้อม High Accuracy Mode
- แสดงค่าความแม่นยำ (Accuracy) ในหน่วยเมตร
- สร้างลิงก์ Google Maps อัตโนมัติ
- รองรับทั้ง GPS, Wi-Fi และ Cell Tower

### ⚡ ฟอร์มที่รวดเร็วและง่าย
- **3 ขั้นตอนเท่านั้น**: กดปุ่ม → กรอกจำนวนคน → ส่ง
- ปุ่ม +/- สำหรับกรอกตัวเลข (เหมาะกับมือถือ)
- แสดงจำนวนรวมแบบ Real-time
- ช่องข้อมูลเพิ่มเติมเป็น Optional

### 👥 จำแนกประเภทผู้ประสบภัย
- **ผู้ใหญ่** - สำหรับผู้ใหญ่ทั่วไป
- **เด็ก** - สำหรับเด็กและเยาวชน
- **ผู้ป่วย/ผู้สูงอายุ** - สำหรับผู้ที่ต้องการความช่วยเหลือพิเศษ

### 📊 บันทึกข้อมูลใน Google Sheets
- ไม่ต้องตั้ง Server เอง
- รองรับผู้ใช้งานจำนวนมากพร้อมกัน
- ฟรี และไม่มีค่าใช้จ่าย
- ทีมสามารถเข้าถึงข้อมูลได้ทันที

### 📱 Progressive Web App (PWA)
- ติดตั้งเป็นแอปบนมือถือได้
- ทำงานได้แม้สัญญาณไม่แรง (Offline-ready)
- Responsive Design ใช้งานได้ทุกหน้าจอ

### 🎨 UX/UI ที่เป็นมิตร
- ออกแบบให้ใช้งานง่ายในสถานการณ์วิกฤติ
- สีสันโดดเด่น เห็นชัดเจน
- Animation และ Loading States ที่ชัดเจน
- รองรับการใช้งานบนมือถือ

## 📁 โครงสร้างไฟล์

```
locationalrt/
├── index.html              # หน้าเว็บหลัก
├── app.js                  # JavaScript สำหรับ Frontend
├── google-apps-script.js   # Backend Script สำหรับ Google Sheets
├── manifest.json           # PWA Manifest
├── service-worker.js       # Service Worker สำหรับ Offline Support
├── icon-192.png           # Icon 192x192px (ต้องสร้างเอง)
├── icon-512.png           # Icon 512x512px (ต้องสร้างเอง)
└── README.md              # เอกสารนี้
```

## 🚀 วิธีการติดตั้งและใช้งาน

### ขั้นตอนที่ 1: เตรียม Google Sheets

1. **สร้าง Google Sheet ใหม่**
   - ไปที่ https://sheets.google.com
   - สร้าง Spreadsheet ใหม่
   - ตั้งชื่อว่า "ระบบขอความช่วยเหลือ - น้ำท่วม"

2. **คัดลอก Sheet ID**
   - ดู URL ของ Google Sheet: `https://docs.google.com/spreadsheets/d/{SHEET_ID}/edit`
   - คัดลอก `SHEET_ID` ไว้

### ขั้นตอนที่ 2: ตั้งค่า Google Apps Script

1. **เปิด Script Editor**
   - ใน Google Sheet ไปที่ Extensions → Apps Script

2. **วางโค้ด**
   - ลบโค้ดเดิมออก
   - เปิดไฟล์ `google-apps-script.js`
   - คัดลอกโค้ดทั้งหมดไปวาง

3. **แก้ไข Configuration**
   ```javascript
   const SHEET_ID = 'YOUR_GOOGLE_SHEET_ID_HERE'; // วางค่า SHEET_ID จากขั้นตอนที่ 1
   ```

4. **Deploy เป็น Web App**
   - คลิก Deploy → New deployment
   - เลือก Type: **Web app**
   - กรอกข้อมูล:
     - Description: "Flood Help API"
     - Execute as: **Me**
     - Who has access: **Anyone**
   - คลิก **Deploy**
   - คัดลอก **Web app URL**

5. **ทดสอบการทำงาน**
   - เปิด Web app URL ในเบราว์เซอร์
   - ควรเห็นข้อความ "✅ ระบบทำงานปกติ"

### ขั้นตอนที่ 3: ตั้งค่า Frontend

1. **แก้ไขไฟล์ app.js**
   - เปิดไฟล์ `app.js`
   - แก้ไขบรรทัด:
     ```javascript
     const CONFIG = {
         GOOGLE_SCRIPT_URL: 'YOUR_GOOGLE_APPS_SCRIPT_URL_HERE',
         // วาง Web app URL จากขั้นตอนที่ 2
     };
     ```

2. **สร้าง Icons สำหรับ PWA**
   - สร้างรูป Icon ขนาด 192x192px และ 512x512px
   - บันทึกเป็น `icon-192.png` และ `icon-512.png`
   - หรือใช้ Tool สร้างแบบออนไลน์: https://www.pwabuilder.com/imageGenerator

### ขั้นตอนที่ 4: Deploy Web Application

#### ตัวเลือก 1: GitHub Pages (ฟรี)

1. สร้าง Repository ใหม่บน GitHub
2. อัพโหลดไฟล์ทั้งหมด
3. ไปที่ Settings → Pages
4. เลือก Source: main branch
5. คลิก Save
6. รอสักครู่ จะได้ URL: `https://yourusername.github.io/repositoryname`

#### ตัวเลือก 2: Netlify (ฟรี, แนะนำ)

1. ไปที่ https://www.netlify.com
2. Drag & Drop โฟลเดอร์ทั้งหมด
3. รอ Deploy เสร็จ
4. ได้ URL ทันที

#### ตัวเลือก 3: Vercel (ฟรี)

1. ไปที่ https://vercel.com
2. Import project
3. Deploy

#### ตัวเลือก 4: Host เองบน Server

1. อัพโหลดไฟล์ทั้งหมดไปยัง Web Server
2. ตั้งค่า HTTPS (สำคัญมาก! Geolocation ต้องใช้ HTTPS)

### ขั้นตอนที่ 5: ทดสอบระบบ

1. เปิด URL ของ Web Application
2. คลิก "🎯 ส่งตำแหน่งของฉัน"
3. อนุญาตให้เข้าถึงตำแหน่ง
4. กรอกจำนวนคน
5. คลิก "📤 ส่งคำขอความช่วยเหลือ"
6. ตรวจสอบใน Google Sheets ว่ามีข้อมูลเข้ามา

## 📊 โครงสร้างข้อมูลใน Google Sheets

| คอลัมน์ | ชื่อ | รายละเอียด |
|---------|------|------------|
| A | เลขที่ | เลขลำดับคำขอ |
| B | วันที่/เวลาที่ส่งข้อมูล | เวลาที่ผู้ใช้ส่งฟอร์ม |
| C | Latitude | พิกัด Latitude (6 ตำแหน่งทศนิยม) |
| D | Longitude | พิกัด Longitude (6 ตำแหน่งทศนิยม) |
| E | ความแม่นยำ (เมตร) | ค่า Accuracy จาก GPS |
| F | Google Maps Link | ลิงก์ดูแผนที่ (คลิกได้ทันที) |
| G | ผู้ใหญ่ | จำนวนผู้ใหญ่ |
| H | เด็ก | จำนวนเด็ก |
| I | ผู้ป่วย/ผู้สูงอายุ | จำนวนผู้ป่วย/ผู้สูงอายุ |
| J | รวมจำนวนคน | จำนวนทั้งหมด |
| K | ข้อมูลเพิ่มเติม | หมายเหตุจากผู้ใช้ |
| L | สถานะ | สถานะการช่วยเหลือ (อัพเดตได้) |
| M | หมายเหตุ | หมายเหตุจากทีมกู้ภัย |
| N | User Agent | ข้อมูล Browser/Device |

## 🔧 การจัดการข้อมูล

### อัพเดตสถานะการช่วยเหลือ

ใช้ฟังก์ชัน `updateStatus()` ใน Google Apps Script:

```javascript
// ไปที่ Apps Script Editor
// เรียกใช้ฟังก์ชันนี้

// ตัวอย่างที่ 1: อัพเดตว่ากำลังดำเนินการ
updateStatus(2, '🚁 กำลังดำเนินการ', 'ทีมกู้ภัยออกเดินทางแล้ว');

// ตัวอย่างที่ 2: อัพเดตว่าเสร็จสิ้น
updateStatus(2, '✅ เสร็จสิ้น', 'ช่วยเหลือสำเร็จ ส่งต่อโรงพยาบาล');

// ตัวอย่างที่ 3: ยกเลิก
updateStatus(2, '❌ ยกเลิก', 'ผู้ประสบภัยอพยพได้เอง');
```

### เพิ่มข้อมูลทดสอบ

```javascript
// ใน Apps Script Editor
// เรียกใช้ฟังก์ชัน testAddSampleData()
testAddSampleData();
```

## 🔒 ความปลอดภัยและข้อควรระวัง

### ความเป็นส่วนตัว
- ข้อมูล GPS มีความละเอียดสูง กรุณาใช้อย่างรับผิดชอบ
- ตั้งค่าสิทธิ์ Google Sheet ให้เฉพาะทีมที่เกี่ยวข้อง
- พิจารณาลบข้อมูลหลังจากสถานการณ์จบแล้ว

### HTTPS เท่านั้น
- Geolocation API ทำงานบน HTTPS เท่านั้น
- ห้ามใช้ HTTP ธรรมดา (จะไม่สามารถตรวจจับตำแหน่งได้)

### ข้อจำกัดของ Google Apps Script
- รองรับ Request ได้สูงสุด ~20 requests/second
- เหมาะกับระบบขนาดกลาง
- หากมีผู้ใช้งานมากกว่า 1000 คน/นาที ควรใช้ Backend อื่น

## 📱 การใช้งานบนมือถือ

### iOS (iPhone/iPad)
1. เปิดเว็บใน Safari
2. กดปุ่ม Share (ไอคอนแชร์)
3. เลือก "Add to Home Screen"
4. แอปจะติดตั้งบน Home Screen

### Android
1. เปิดเว็บใน Chrome
2. กดปุ่มเมนู (3 จุด)
3. เลือก "Add to Home Screen" หรือ "Install App"
4. แอปจะติดตั้งเหมือนแอปทั่วไป

## 🎨 การปรับแต่ง

### เปลี่ยนสีธีม
แก้ไขใน `index.html`:

```css
.header {
    background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
}

body {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### เพิ่มฟิลด์ใหม่
1. เพิ่ม HTML input ใน `index.html`
2. เพิ่มการจัดการใน `app.js`
3. เพิ่มคอลัมน์ใน `google-apps-script.js`

### เพิ่มการแจ้งเตือน (Email/Line)
แก้ไขฟังก์ชัน `sendNotification()` ใน `google-apps-script.js`:

```javascript
function sendNotification(data, requestNumber) {
  // ส่ง Email
  const email = 'team@example.com';
  const subject = `🆘 คำขอใหม่ #${requestNumber}`;
  const body = `มีคำขอความช่วยเหลือใหม่...\n\nพิกัด: ${data.latitude}, ${data.longitude}`;

  MailApp.sendEmail(email, subject, body);

  // หรือส่งผ่าน Line Notify
  // UrlFetchApp.fetch('https://notify-api.line.me/api/notify', {
  //   method: 'post',
  //   headers: { 'Authorization': 'Bearer YOUR_LINE_TOKEN' },
  //   payload: { message: body }
  // });
}
```

## 🐛 การแก้ไขปัญหา

### ปัญหา: ตรวจจับตำแหน่งไม่ได้

**สาเหตุและแนวทางแก้ไข:**

1. **ไม่ใช้ HTTPS**
   - ✅ ใช้ HTTPS เท่านั้น (Geolocation API บังคับ)

2. **ไม่อนุญาตสิทธิ์**
   - ✅ ตรวจสอบว่าอนุญาตให้เข้าถึงตำแหน่ง
   - ใน Chrome: Settings → Privacy → Site Settings → Location

3. **GPS ไม่เปิด**
   - ✅ เปิด Location Services ในมือถือ

4. **สัญญาณไม่ดี**
   - ✅ ออกไปนอกอาคาร หรือใกล้หน้าต่าง

### ปัญหา: ส่งข้อมูลแล้วไม่เข้า Google Sheets

**สาเหตุและแนวทางแก้ไข:**

1. **URL ผิด**
   - ✅ ตรวจสอบว่า `CONFIG.GOOGLE_SCRIPT_URL` ถูกต้อง
   - ต้องเป็น URL แบบ: `https://script.google.com/macros/s/.../exec`

2. **Sheet ID ผิด**
   - ✅ ตรวจสอบ `SHEET_ID` ใน Apps Script

3. **Permissions**
   - ✅ Deploy ใหม่ และอนุญาตสิทธิ์ทั้งหมด

4. **Script ไม่ทำงาน**
   - ✅ ดู Logs ใน Apps Script: Executions
   - ✅ ทดสอบด้วย `testAddSampleData()`

### ปัญหา: PWA ติดตั้งไม่ได้

**สาเหตุและแนวทางแก้ไข:**

1. **ไม่ใช้ HTTPS**
   - ✅ PWA ต้องใช้ HTTPS

2. **ไม่มี icons**
   - ✅ ต้องมีไฟล์ icon-192.png และ icon-512.png

3. **Service Worker ไม่ทำงาน**
   - ✅ เปิด DevTools → Application → Service Workers
   - ✅ ตรวจสอบ Console ว่ามี error หรือไม่

## 📈 การเพิ่มประสิทธิภาพ

### สำหรับระบบขนาดใหญ่

หากมีผู้ใช้งานมากกว่า 1,000 คนพร้อมกัน แนะนำ:

1. **ใช้ Firebase Realtime Database**
   - รองรับ Concurrent Users สูงกว่า
   - Real-time updates

2. **ใช้ Cloud Functions**
   - Scale อัตโนมัติ
   - ไม่มีข้อจำกัด requests/second

3. **ใช้ Cloudflare Workers**
   - กระจาย Load ทั่วโลก
   - ความเร็วสูงมาก

4. **เพิ่ม Queue System**
   - ใช้ Redis หรือ RabbitMQ
   - ป้องกัน Rate Limiting

## 🤝 การพัฒนาต่อ

### Features ที่แนะนำให้เพิ่ม

- [ ] แผนที่แสดงตำแหน่งผู้ประสบภัยทั้งหมด
- [ ] ระบบแชทระหว่างผู้ประสบภัยกับทีมกู้ภัย
- [ ] การแจ้งเตือนแบบ Real-time
- [ ] ระบบ Queue สำหรับการช่วยเหลือ
- [ ] Dashboard สำหรับทีมกู้ภัย
- [ ] รองรับหลายภาษา (อังกฤษ, ลาว, กะเหรี่ยง)
- [ ] อัพโหลดรูปภาพสถานการณ์
- [ ] ระบุระดับความเร่งด่วน
- [ ] ระบบ Check-in ว่าปลอดภัยแล้ว

## 📄 License

โปรเจกต์นี้สร้างขึ้นเพื่อช่วยเหลือสังคมในสถานการณ์ฉุกเฉิน
สามารถนำไปใช้และปรับแต่งได้อย่างอิสระ

## 🙏 ขอบคุณ

สร้างด้วยความตั้งใจเพื่อช่วยเหลือผู้ประสบภัยในสถานการณ์น้ำท่วม

---

**หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อผู้พัฒนา**